VITE_API_URL=http://127.0.0.1:8000/api

# --- Docker (docker-compose.yml) --------------------------------------------------------------
# Porta do host pro admin — a VPS já roda outras aplicações, troque se 8080 colidir com alguma.
ADMIN_PORT=8080
